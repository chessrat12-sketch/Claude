# 시뮬레이션 코어 + 오프라인 데모/테스트는 의존성이 없다 (순수 표준 라이브러리).
# 아래는 선택 사항.

# pytest>=7.0          # 테스트 러너 (tests/ 는 단독 실행도 가능)
# numpy>=1.24.0        # 대규모 지표 분석
# matplotlib>=3.7.0    # 가격 수렴·신뢰 그래프 시각화
#
# LLM 백엔드는 표준 라이브러리 urllib 로 OpenAI 호환 엔드포인트를 호출하므로
# 별도 SDK가 필요 없다. 환경변수: EC_LLM_BASE_URL, EC_LLM_MODEL, EC_LLM_API_KEY
